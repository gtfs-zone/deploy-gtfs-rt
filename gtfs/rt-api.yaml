# ── rt-api (cafe-car) ─────────────────────────────────────────────────────────
# The single cafe-car image runs as two Deployments, matching the TF split:
#   gtfs-api      (:8000, public)  — unauthenticated GTFS-RT feed at rt.gtfs.zone
#   gtfs-manager  (:8001, admin)   — oauth2-protected UI at manage.rt.gtfs.zone
# Shared env mirrors the TF `local.rt_api_env`. The DATABASE_URL password is
# injected via a dependent env var ($(POSTGRES_RT_API_PASSWORD)); Postgres host is
# CNPG's read-write Service `postgres-rw`. Alembic migrations run separately as the
# PreSync Job in rt-api-migrate.yaml. The `/run/nanomq` shared volume is gone with
# the MQTT drop.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gtfs-api
  namespace: gtfs
  labels:
    app: gtfs-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gtfs-api
  template:
    metadata:
      labels:
        app: gtfs-api
    spec:
      containers:
        - name: gtfs-api
          image: git.kcfam.us/gtfs.zone/cafe-car:1b047c7
          command: ["fastapi", "run", "src/cafe_car/main.py", "--port", "8000"]
          ports:
            - name: http
              containerPort: 8000
          env:
            - name: REDIS_URL
              value: redis://redis:6379/1
            - name: CELERY_BROKER_URL
              value: redis://redis:6379/3
            - name: OAUTH2_PROXY_LOGOUT_URL
              value: https://auth.gtfs.zone/oauth2/sign_out?rd=https://manage.rt.gtfs.zone
            - name: POSTGRES_RT_API_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-rt-api
                  key: password
            - name: DATABASE_URL
              value: postgresql://rt_api:$(POSTGRES_RT_API_PASSWORD)@postgres-rw:5432/rt_api
            - name: SESSION_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: gtfs-app-secrets
                  key: SESSION_SECRET_KEY
            # Traccar device provisioning. cafe-car auto-creates a Traccar device
            # per Tracker over REST using the admin account, and bakes
            # TRACCAR_CLIENT_BASE into the org.traccar.client://config deep link
            # it renders as a QR — so that URL must be reachable from the driver's
            # phone, not just from inside the cluster.
            - name: TRACCAR_URL
              value: http://traccar:8082
            - name: TRACCAR_CLIENT_BASE
              value: https://traccar.gtfs.zone/osmand
            - name: TRACCAR_EMAIL
              valueFrom:
                secretKeyRef:
                  name: gtfs-app-secrets
                  key: TRACCAR_ADMIN_EMAIL
            - name: TRACCAR_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gtfs-app-secrets
                  key: TRACCAR_ADMIN_PASSWORD
            # Shared bearer guarding /ingest/* (hell-gate-bridge is the producer).
            - name: INGEST_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: gtfs-app-secrets
                  key: INGEST_API_TOKEN
            # viz.rt.gtfs.zone (test-track) reads GET /feeds to populate its Load
            # modal, and that is a browser fetch from a different origin.
            - name: CORS_ALLOWED_ORIGINS
              value: '["https://manage.rt.gtfs.zone","https://rt.gtfs.zone","https://viz.rt.gtfs.zone"]'
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: gtfs-api
  namespace: gtfs
spec:
  selector:
    app: gtfs-api
  ports:
    - name: http
      port: 8000
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gtfs-manager
  namespace: gtfs
  labels:
    app: gtfs-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gtfs-manager
  template:
    metadata:
      labels:
        app: gtfs-manager
    spec:
      containers:
        - name: gtfs-manager
          image: git.kcfam.us/gtfs.zone/cafe-car:1b047c7
          command:
            - fastapi
            - run
            - src/cafe_car/admin_main.py
            - --port
            - "8001"
            - --proxy-headers
            - --forwarded-allow-ips=*
          ports:
            - name: http
              containerPort: 8001
          env:
            - name: REDIS_URL
              value: redis://redis:6379/1
            - name: CELERY_BROKER_URL
              value: redis://redis:6379/3
            - name: OAUTH2_PROXY_LOGOUT_URL
              value: https://auth.gtfs.zone/oauth2/sign_out?rd=https://manage.rt.gtfs.zone
            - name: POSTGRES_RT_API_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-rt-api
                  key: password
            - name: DATABASE_URL
              value: postgresql://rt_api:$(POSTGRES_RT_API_PASSWORD)@postgres-rw:5432/rt_api
            - name: SESSION_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: gtfs-app-secrets
                  key: SESSION_SECRET_KEY
            # Traccar device provisioning. cafe-car auto-creates a Traccar device
            # per Tracker over REST using the admin account, and bakes
            # TRACCAR_CLIENT_BASE into the org.traccar.client://config deep link
            # it renders as a QR — so that URL must be reachable from the driver's
            # phone, not just from inside the cluster.
            - name: TRACCAR_URL
              value: http://traccar:8082
            - name: TRACCAR_CLIENT_BASE
              value: https://traccar.gtfs.zone/osmand
            - name: TRACCAR_EMAIL
              valueFrom:
                secretKeyRef:
                  name: gtfs-app-secrets
                  key: TRACCAR_ADMIN_EMAIL
            - name: TRACCAR_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: gtfs-app-secrets
                  key: TRACCAR_ADMIN_PASSWORD
            # Shared bearer guarding /ingest/* (hell-gate-bridge is the producer).
            - name: INGEST_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: gtfs-app-secrets
                  key: INGEST_API_TOKEN
            - name: CORS_ALLOWED_ORIGINS
              value: '["https://manage.rt.gtfs.zone","https://rt.gtfs.zone"]'
---
apiVersion: v1
kind: Service
metadata:
  name: gtfs-manager
  namespace: gtfs
spec:
  selector:
    app: gtfs-manager
  ports:
    - name: http
      port: 8001
      targetPort: http
