auth {
    allow_anonymous = false
    no_match = deny
    deny_action = disconnect

    password = {include "/run/nanomq/passwd"}
    acl = {include "/etc/nanomq-acl.conf"}
}

listeners.tcp {
    bind = "0.0.0.0:1883"
}

listeners.ws {
    bind = "0.0.0.0:8083/mqtt"
}
